# Some Notes About Keyboard Controls

There are no keyboard inputs for `clear`, `sign`, and `percent` buttons. As for multiple and add, the corresponding keyboard inputs are `x` and `p` respectively.
